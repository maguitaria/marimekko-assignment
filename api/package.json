{
  "name": "marimekko-wholesale-api",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p .",
    "start": "func start",
    "dev": "npm run build && npm run start",
    "deploy:prepare": "npm ci && npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/jsonwebtoken": "^9.0.6"
  }
}
